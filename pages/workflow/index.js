import { Box, Stack } from "@mui/system"

const Workflow = ({}) => {
    return (
        <Stack spacing={4} sx={{ marginTop: '24px' }}>
            <Box
                component='h6'
                sx={{ fontSize: '40px' }}
            >
                Workflow Page
            </Box>
        </Stack>
    )
}

export default Workflow
