import { styled } from "@mui/system"
import { Layout } from "antd"

const CustomContent = styled(Layout.Content)`
    background-color: rgba(255, 255, 255, 0.90);
    min-height: calc(100vh - 148px);
`

export default CustomContent
